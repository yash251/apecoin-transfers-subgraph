{
  "name": "apecoin-transfers-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ apecoin-transfers-subgraph",
    "create-local": "graph create --node http://localhost:8020/ apecoin-transfers-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ apecoin-transfers-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 apecoin-transfers-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
